
name: authentication_repository
description: Dart package which manages the authentication domain.
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  equatable: ^2.0.0
  base_http:
    path: ../base_http
